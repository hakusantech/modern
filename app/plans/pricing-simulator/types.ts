// Types definitions for the pricing simulator

export type PlanType = "basic" | "standard" | "premium";
export type BusinessType = "hotel" | "minpaku";
export type Option = {
  id: string;
  label: string;
};